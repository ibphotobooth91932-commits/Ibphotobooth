# Ibphotobooth

Starter repository for the Ibphotobooth project. This initial commit creates a README so the repository has a default branch; follow-up commits will add a Jekyll site and Pages workflow on a new branch.

## Published Site

The site has been pushed to the GitHub repository `ibphotobooth91932-commits/Ibphotobooth`.

A custom domain has been configured via `CNAME`: `www.ibphotobooth.com`.

Once GitHub Pages is enabled from repository settings, the live site should be available at:

`https://www.ibphotobooth.com`

> Note: GitHub Pages was not enabled automatically from this environment because the token does not allow Pages configuration. Please enable Pages manually under repository settings if it is not already active.